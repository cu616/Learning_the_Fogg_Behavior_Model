# 项目协作说明

本项目用于学习福格行为模型，并开发一款本地优先的行为设计软件。

## 开始工作前

后续 AI Agent 在进行产品设计或实现前，应依次阅读：

1. `docs/README.md`
2. `docs/blueprints/软件大纲蓝图.md`
3. `docs/decisions/` 中所有状态为“已接受”的决策记录
4. 与当前任务相关的 `knowledge/` 分章资料

## 项目约束

- 以书中的行为设计七步骤作为软件主工作流。
- 软件必须本地优先；不得在未经用户明确同意的情况下把个人习惯数据发送到外部服务。
- 核心七步流程必须在离线状态下可用，不应依赖 AI 或网络服务才能完成。
- 软件支持管理多个习惯；每个习惯保存独立的设计状态、配方版本、实践日志和诊断记录。
- 不把中断、忘记或调整配方描述为用户“失败”。
- 不以连续打卡、惩罚或羞耻感作为主要行为机制。
- 高频迭代阶段只需更新可直接运行的 `app/src-tauri/target/release/app.exe`；除非用户明确提出“打包、安装包或准备发布”，不要每轮都生成 NSIS 安装包。

## 重大决策记录

任何会显著影响产品方向、数据结构、技术架构、隐私、安全、工作流或用户体验的决定，都必须：

1. 在 `docs/decisions/` 新建或更新一份 ADR；
2. 写明背景、决定、理由、影响和备选方案；
3. 在实现前确认其状态；
4. 如果推翻旧决定，不删除旧文档，而是新增替代记录并建立链接。

文件命名采用：`ADR-NNNN-简短标题.md`。

## 知识库维护

- `source/福格行为模型.pdf`、`knowledge/福格行为模型-全文.md`、`knowledge/chapters/` 和 `knowledge/page-index.jsonl` 均纳入 Git，方便克隆后直接学习和检索。
- `knowledge/福格行为模型-全文.md`、`knowledge/chapters/` 和 `knowledge/page-index.jsonl` 是生成内容。需要重新提取时运行 `scripts/extract_fogg_book.py`，不要手工批量改写生成文件；原书、全文、分章和索引应作为同一组变更提交。
- 仓库所有者负责确认原书和生成内容的上传、存储与分发授权。若授权状态改变，应停止分发并新增 ADR 记录处理方式。
- `knowledge/references/`（庆祝方式库、微习惯配方库）由 `scripts/extract_references.py` 从附录生成，更新时重跑该脚本，不要手工批量改写。
- 附录表 9-1「32 种肯定成功的方式」为图片，已人工转录为 `knowledge/references/肯定成功方式库.md` 和 `.json`；修改时必须同步更新两种格式并对照 PDF 第 328-329 页。
- 对书中观点进行产品化解释时，优先记录 PDF 物理页码，方便回查原始资料。
