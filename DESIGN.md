---
name: 福格行为实验室
description: 本地优先、以当前行动为中心的行为设计桌面工作台
colors:
  panel: "#f1f6f6"
  canvas: "#dfe9ec"
  ink: "#26363d"
  muted: "#607178"
  line: "#bdced3"
  accent: "#2e6f7e"
  accent-dark: "#245865"
  accent-soft: "#cee4e8"
  step-1: "#6f5a9f"
  step-1-soft: "#eeeaf7"
  step-2: "#317681"
  step-2-soft: "#e5f1f2"
  step-3: "#34668f"
  step-3-soft: "#e6eef6"
  step-4: "#995b35"
  step-4-soft: "#f7eae2"
  step-5: "#527044"
  step-5-soft: "#eaf1e6"
  step-6: "#896817"
  step-6-soft: "#f8efd7"
  step-7: "#80556b"
  step-7-soft: "#f2e7ec"
  positive: "#3c8065"
  positive-soft: "#e1f0e8"
  warm: "#fff3d8"
  danger: "#a94e45"
typography:
  sans:
    fontFamily: "Segoe UI, Microsoft YaHei, system-ui, sans-serif"
---

# 福格行为实验室·孤独摇滚主题版界面设计规范

## 产品与设计意图

这是一个本地优先的个人行为设计工作台，不是数据后台，也不是用打卡压力推动用户的任务管理器。页面首先帮助用户完成当前一个设计动作，其次显示本次流程的位置与已经形成的结果，最后才提供理论笔记和参考资料。

界面气质应当清楚、可信，并具有《孤独摇滚！》青春乐队漫画的节奏。当前主题使用粉、黄、浅蓝、黑四色、粗线和错位硬阴影，不依赖在线字体、图片、图标或设计资源；避免通用 SaaS 仪表盘式统计卡、玻璃拟态和微型英文眉题配巨大标题。

### 孤独摇滚主题分支

- 当前方向由 [ADR-0036](./docs/decisions/ADR-0036-孤独摇滚主题视觉重构.md) 定义；早期浅湖绿和深色玻璃规范均已被替代。
- `codex/anime-journal-ui` 使用《孤独摇滚！》动画官网透明角色立绘和青春乐队漫画语言：粉、黄、浅蓝、黑，粗线、错位硬阴影、网点纸张和非对称站位。
- 项目页固定使用“顶部状态票据—中央漫画章节—右侧角色旁白”。状态不再常驻左栏，角色台词随步骤切换，并用排练比喻解释福格方法。
- 角色不作为奖惩、监督、完成率评价或连续打卡反馈；暂停、再次发生和调整配方仍是中性的设计信息。
- 展示标题使用本机方正姚体，正文使用 `Noto Sans SC` / 微软雅黑；产品名约 34px，页面标题 28–32px，项目标题 19px，正文 17px，输入和按钮不小于 16px，辅助信息不小于 14px。
- 所有具体 IP 素材集中放在 `app/public/themes/kessoku/` 并记录官方来源。核心流程不得通过网络加载主题图片。
- 手机端首页保留不遮挡控件的角色构图；工作台把全身人物降级为台词条中的角色小像。任何输入、导航、错误和主要动作都保持清晰。

## 阅读层级

每个工作台页面固定建立三层阅读目标：

1. 第一目标是中央工作区的当前步骤标题、当前问题和主要输入或动作。
2. 第二目标是顶部步骤导航，以及左侧常驻的设计状态或一次性行为状态。
3. 第三目标是按需打开的行为设计排练册、参考库和历史记录。

第一目标使用最大字号、最充足留白和步骤强调色。第二目标降低对比度并保持紧凑。第三目标默认不占据画布，只在用户主动请求时出现。

## 布局

### 当前行动首页

- 首页先显示“继续进行”，最多并列三个最近更新的未归档对象；卡片同时说明工作流类型、当前阶段和下一动作。
- 最近项目只按更新时间排序，不使用连续天数、完成率或惩罚性颜色。状态数量只用于快速定位。
- 三类工作流分区是第二阅读层，默认收起并记住本机展开偏好。分区标题需同时提供用途说明和当前结果数。
- 搜索与状态筛选放在“管理行为设计”标题旁，不影响上方最近行动排列。窄窗口中两个控件回退为单列。
- 首页的主要信息容器使用较稳定的深色材质，项目分区使用更通透的第二层材质；长期、一次性和终止行为只用青绿、暖砂和灰玫瑰小面积标识。

### 离线使用说明

- 使用说明是可随时进入、随时退出的静态学习页，不得成为新建项目前的强制流程。
- 宽屏使用左侧粘性章节导航和右侧单列长文；窄窗口改为水平滚动的紧凑章节导航，正文保持单列。
- 首屏先用现实语言解释行为发生条件；首次出现英文术语时写出 `Motivation · Ability · Prompt` 全称，不把 `MAP` 或 `P / A / M` 单字母做成大号视觉标题。后续操作区统一用“动机、能力、提示”中文名称。
- 后续依次展示工作流选择、七步案例、现实诊断和终止行为边界，不按原书章节堆叠摘要。
- 七步案例逐步给出“生活情境、本步问题、现在做什么、示例结果、可以继续的判断”；说明页还必须提供一次性行为演练、可照抄句式和从真实现场出发的卡点处理建议。
- 说明页不读取、更改或展示个人行为数据，不使用网络图片、在线字体或外部教程。

### 聚焦式工作台

- 中央工作区是主要操作区，宽桌面上约占可用空间的三分之二以上。
- 顶部仅保留返回、项目名称、流程位置、摘要开关、笔记开关和保存状态等全局信息。
- “设计状态”是默认展开、可收起的顶部结果票据。它随七步逐渐形成，允许用户边操作边对照愿望、黄金行为、基线行为、锚点、庆祝和配方。
- 右侧笔记是覆盖式抽屉，默认关闭。打开时覆盖画布右侧而不是挤压中央区，并提供遮罩、关闭按钮、Esc 关闭和焦点回收。
- 一次性行为的三段路径固定显示在中央区上方的紧凑导航中；左侧常驻显示当前下一动作和最近调整。
- 终止旧习惯使用同一骨架：顶部为五阶段紧凑导航，左侧常驻显示概括型旧习惯、具体行为、当前目标、P/A/M 对策数量和观察记录；中央按“准备—拆解—对策—观察—替代”聚焦当前任务。
- 窄窗口中状态票据保持在正文上方横向滚动，不覆盖角色台词和主要输入；任何支持面板都不得让中央内容缩到难以阅读。

### 间距与分组

- 优先用 8、12、16、24、32 像素的间距层级表达相邻、成组和分区关系。
- 同一任务中的连续字段使用留白和小标题组织，不为每一个字段套独立浅色卡片。
- 只在需要独立点击、强调结果、警告或承载浮层时使用边框和有底色的容器。
- 主内容最大阅读宽度保持在约 980 像素，短表单优先双列，窄屏回退单列。

## 色彩

- `ink` 用于当前任务、标题和主要结果；`muted` 只用于非关键说明。
- `accent` 用于首页、一次性行为和跨流程控制，不主导长期七步工作台。
- 长期习惯七步分别使用紫、青蓝、深蓝、陶土、橄榄、赭黄和玫瑰色。七种深色令牌保持相近明度与饱和度，并各有一个浅色承托令牌。
- 当前步骤色用于步骤导航、主要动作、输入焦点、标题标记与中央工作面顶部的轻微色调；大面积正文和表面仍使用中性色。
- 左侧状态结果按形成它的步骤固定着色：愿望使用第一步色，黄金行为使用第三步色，微习惯使用第四步色，锚点使用第五步色，庆祝与完整配方使用第六步色。
- 色彩不能单独表达完成、当前或未开始状态；同时使用文字、图标、填充强度或边框区分。
- `positive` 表示形成、完成或稳定，不表示连续打卡奖惩；`danger` 只用于不可逆操作和错误。
- `canvas` 覆盖窗口底层，`panel` 承载中央工作面；纯白只用于需要从工作面抬起的少数交互卡片。
- `line` 应谨慎使用。能用间距或画布与工作面的色差区分的内容不再添加边框。

## 字体与图标

- 使用本机 `Segoe UI`、`Microsoft YaHei` 和系统无衬线字体，保证离线与中文清晰度。
- 页面主标题约 28–32 像素，区块标题约 19–26 像素，正文与说明采用 17 像素，输入和按钮不小于 16 像素，坐标、状态和元信息不小于 14 像素。
- 空白交互画布必须说明“现在能做什么”。第二步和第三步在没有行为条目时压缩高度并显示具体入口，生成内容后再恢复完整工作空间。
- 高频图标使用内联 SVG，统一 18–20 像素、1.8 像素线宽、圆角端点。不可只靠符号字形表达关键动作。
- 图标按钮必须有 `title`、`aria-label` 和清晰的键盘焦点。

## 组件行为

### 设计状态栏

- 宽桌面默认展开，由顶部状态按钮收起或恢复；用户操作中央表单时始终可以对照。
- 内容只显示已经形成的愿望、黄金行为、微习惯方案和关键结果，不重复未来步骤说明，也不使用“摘要”这一名称弱化它的流程地位。
- 分区主要使用留白与短标签；基线与可选扩展等短结果可并排。

### 行为设计排练册抽屉

- 抽屉标题固定在顶部，正文独立滚动，关闭动作始终可见。
- 内容使用手风琴渐进披露。默认仅展开第一个最相关条目，其余只显示标题。
- 抽屉先给当前结论，再用“看现实信号—问一个问题—带回现实试一次”引导使用；每条展开内容必须提供一个可执行动作。
- 界面不显示原书 PDF 页码、章节定位或书目式尾注；回查证据保留在项目知识库和设计文档中。
- 展开状态只影响当前界面，不改变用户行为数据。

### 表单与卡片

- 当前关键输入可以有一处柔和强调面；普通字段应直接排列在内容流中。
- 每页只保留一个最突出的主要动作。次要动作使用普通按钮或折叠区。
- 长解释、历史记录、可选字段和参考库默认折叠，不推动主任务离开首屏。
- 一次性行为的 P、A、M 解决页按“判断对象—选择方法—写下调整—决定去向”逐段展开；当前阶段完成前不显示后续字段，已经形成的选择保留在上方供核对。
- 与时间、地点或最低动机测试有关的字段只在所选方法需要时出现。“其他方法”必须转写成具体、可执行的文本。
- 一次性提示默认只是保存在本机的逻辑方案；界面不得用时间输入框暗示尚未实现的系统通知能力。
- 终止旧习惯的 P/A/M 地图始终显示全部方法标题与已有内容，但一次只展开一个新增区。观察页先呈现五个快速结果，只有旧行为再次发生时才展开提示、未覆盖情境和对策回写。
- 终止旧习惯的具体行为集群复用长期习惯的空间语言：中心是概括型旧习惯，外围具体行为卡片可拖动和缩放并保存到本机。拖拽与缩放都必须提供方向键替代操作。
- 长期习惯第二步在桌面端保留可拖拽行为画布；手机端改为愿望卡后的纵向候选行为清单，并隐藏在窄屏上无效的拖拽和缩放控制。
- 具体旧行为只常显改变目标与目标范围；典型时间、地点、人物、情境和选择理由默认收进“补充典型情境”折叠区。
- 完成一个具体旧行为后返回行为集群选择下一个行为，不强制结束项目或返回首页。
- 终止旧习惯使用克制的灰玫瑰强调色，与中性骨架和既有七步色谱协调；它用于流程定位，不铺满页面。

### 表面层级

- 窗口底层使用随应用内置的低饱和蓝灰山景，固定铺满并叠加蓝灰暗层；背景不动画、不联网、不参与状态表达。
- 材质按角色区分并形成连续阶梯：背景暗层约 20%，顶栏和首页分类区约 26%，左侧状态栏与普通按钮约 40% 黑色，中央工作面与项目卡约 48% 黑色；文本输入在所在工作面上使用约 8% 的中性亮色叠层，聚焦时约 12%；只有理论抽屉和参考库浮层使用约 68% 的稳定深色。
- 中央工作面必须保持最高可读性。只有最外层角色表面使用背景模糊，内部操作卡片使用低强度亮色叠层、单像素边界和轻阴影，不重复模糊。
- 首页三类项目区继续用青绿、暖砂和灰玫瑰作为小面积语义强调；正文使用统一的高对比白色和两级灰白文字。
- 七步主题色使用适合深色界面的高明度低饱和色阶，只在步骤标题、左侧当前结果和被选行为这三类语义锚点提供淡色承托；普通内容不得重复铺彩色卡片。
- 表面之间首先依靠色差和间距分层，边框只补充边界；正常正文在所在表面上保持至少 4.5:1 对比度。
- Dock 使用约 20px 模糊，Sidebar 和稳定 Glass Card 最外层可使用约 40px 模糊；任何嵌套卡片不得再次模糊。系统要求减少透明度时回退到不透明深灰表面。

## 动效与无障碍

- 面板开合使用约 160–220 毫秒的位移和透明度变化；支持 `prefers-reduced-motion`。
- 抽屉打开后焦点进入关闭按钮，Tab 不应进入被遮挡内容；关闭后焦点返回触发按钮。
- 遮罩可点击关闭，Esc 可关闭；按钮、输入与手风琴均可通过键盘操作。
- 正文与背景保持可读对比度，颜色不能成为状态的唯一表达方式。

## 应做与不应做

应做：让用户打开页面后先看到当前问题；让设计状态始终可对照；把理论信息留在按需抽屉；用留白建立组别；保持长期与一次性工作流的同一结构语言。

不应做：让左右两块长文字同时争夺注意；用纯白铺满窗口或用大量同权浅色卡片铺满画布；为每个区域重复解释流程；为了“好看”引入在线字体、装饰渐变或不一致的表情符号图标。
