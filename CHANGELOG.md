# 工作日志（Changelog）

本文档按时间顺序记录本项目的开发工作与里程碑。

---

## 2026-08-24 · Android 0.1.1 手机布局修复

- 手机工作台顶栏改为两层：返回、项目名和两个辅助入口位于首行，七步或旧习惯流程独占第二行；右上角按钮扩大到 44px，并加入 Android 安全区边距。
- 长期习惯、一次性行为和终止旧习惯的状态区在窄屏默认收起；展开后改为整宽纵向抽屉，不再用超宽横条把方案操作挤到屏幕外。
- 主工作区、方法抽屉与底部操作栏使用动态视口高度和独立滚动，底部兼顾手势导航安全区；能力链从左侧开始横向滚动，不再居中裁掉首尾选项。
- 应用版本提升至 `0.1.1`，重新生成 ARM64 APK；交付包大小为 26,834,004 字节，SHA-256 为 `748D55F8BC12E7B01284F5368D4B99CAC8B599B41B854DF984CD65C1B204121E`。

---

## 2026-08-24 · Windows 与 Android 构建包

- 新增 ADR-0039，确定 Windows 便携 EXE 与 Android ARM64 APK 的移动端边界；移动端继续使用本机 SQLite。
- 新增 ADR-0040，记录安装包、调试签名与后续正式签名的维护方式。
- 生成带 WebView2 离线组件的 Windows NSIS Setup；安装程序保留“福格行为实验室”名称，并提供快捷方式与卸载入口。
- 应用图标改为粉色拨片、黄色闪电、青蓝节拍线组成的漫画乐队徽章；同步生成 Windows、Android 与高分辨率图标资源，不使用 AI 人物或角色头像。
- 重新构建 Windows 便携版 `app.exe`，并完成 8 秒启动冒烟测试；换用新图标后的便携版 SHA-256 为 `5EC39E4584AC49AE8496AC2E7C6699DCEA8B98BE9D30372CC836064105BF40EA`，Setup SHA-256 为 `174D777701131824F7DFCEA0F596B0FA5E2D86A2024AA5DCDC26D2A807B8DD0C`。
- 前端生产构建改用 Vite runner 配置加载器，避免受限 Windows 工作区中配置打包器向父目录探测并触发权限错误。
- 经用户明确同意后接受全部 Android SDK 许可，配置 OpenJDK 17、Android SDK 36、NDK 29 与 Rust ARM64 目标；生成 Android 7.0 及以上、`arm64-v8a` 架构的 APK。
- APK 已通过包名、版本、最低系统、CPU 架构和 v2 签名校验；交付包大小为 38,066,020 字节，SHA-256 为 `A18588F4EC4FC06E3B546E327C88F2BB9F430313D89EC0F441A9C19C0A1B2ABF`。当前使用 Android 调试证书签名，尚未创建需要长期保管的正式发布密钥。
- 将 Setup、便携版 EXE 与 APK 统一整理到 `output/packages/`，并附安装、备份、更新和签名说明。

---

## 2026-08-23 · 当前行动首页与零基础使用说明

- 修复浏览器演示模式中微习惯方案“重命名、标记稳定、暂停、继续实践、删除”按钮无响应；补齐对应的新建、更新和删除命令，并纳入交互回归。
- 使用说明撤掉口号式开场和 `B = MAP` / `P · 提示` 这类缩写拼贴：首次介绍改用 `Motivation · Ability · Prompt` 全称，操作与诊断区改用自然中文名称。
- 依照 ADR-0038 从界面删除“原书定位 / PDF 物理页”尾注；回查证据留在知识库，行为设计排练册只保留当前判断、自问问题、要点和立即动作。
- 软件内说明改用办公室久坐、预约年度体检和睡前刷短视频三个有现实约束的案例；三张模板式工作流卡改为决策路线板，泛化常见问题改为六段真实现场建议，并移除“本轮只改”等生硬教程口吻。
- 顶部设计状态中的“当前微习惯方案”不再拆成基线、扩展等小卡片，改为可直接朗读的一段配方与一行自然扩展提示。
- 数据与隐私页拆离首页大型头图骨架，改为紧凑页头、局部山田凉角色场景以及备份、迁移、操作保护和参考库四个工作区。
- 补齐终止旧习惯与数据页的浏览器演示命令，使旧习惯项目可进入并切换五个阶段，备份和个人参考库可在本地预览中交互。
- 手机端第二步不再压缩桌面自由画布，改为“愿望卡 + 纵向候选行为清单”；同时修复顶部状态票据覆盖人物台词、首页按钮遮挡角色和候选卡灰黑不可读的问题。
- 补齐长期习惯浏览器演示数据的项目、愿望、行为、焦点地图、黄金行为、分支和配方读取链路；本地预览现在可以从首页正常进入七步工作台。
- 修复原生状态下拉框深色选项导致的黑底黑字，并增加本地主题 favicon；Playwright 桌面和 390px 手机视图检查均无控制台错误。
- 新增 ADR-0037；本轮只构建并验收前端，不生成 EXE 或安装包。
- 放弃上一版《交响诗篇》浅青后台主题，新增《孤独摇滚！》青春乐队漫画主题；四位官方透明角色立绘进入首页海报、当前行动和各工作台，不再使用带说明文字的立绘或裁切群像横条。
- 全局改为粉、黄、浅蓝、黑四色，使用粗黑描边、错位硬阴影、网点纸张和非对称人物站位；首页、长期七步、一次性行为、终止旧习惯和离线说明书统一视觉语言。
- 项目页由“左侧状态栏 + 中央大表单”改为“顶部状态票据 + 中央漫画章节 + 右侧角色旁白”；四位角色按当前步骤给出与福格方法契合的乐队排练比喻，但不监督或评判用户。
- 标题改用本机方正姚体离线展示字，正文保留 Noto Sans SC / 微软雅黑；正文统一为 17px、输入和按钮不小于 16px、辅助与坐标信息不小于 14px，移除微型英文眉题与普通办公黑体式标题。
- 使用说明的“终止旧习惯”由四张同规格模板卡改为错位漫画时间线；第 2 步行为集群和第 3 步焦点地图移除灰色巨幅空状态，改为彩色工作面、明确入口和内容生成后展开。
- 顶部“设计状态”增加可点击七步进度带，利用原来的横向空白呈现实际流程信息。
- 从 OpenAI 官方精选目录安装 `playwright` 与 `security-best-practices` skill；它们将在下一轮 Codex 任务重新加载后用于交互回归和安全审查。
- 保留一次性行为浏览器 mock 修复：读取、保存、诊断、事件等命令可在本地预览中正常工作。
- 首页新增“继续进行”主层，按最近更新统一呈现长期习惯、一次性行为和终止旧习惯，直接显示当前阶段和下一动作。
- 三类管理分区继续按需展开，并记住本机展开偏好；新建后直接进入对应工作台。
- 修正状态筛选在三类对象上的映射，“全部”默认不显示归档，归档项目可统一筛选和恢复；搜索同时覆盖名称、愿望和下一动作。
- 增加页内加载、错误重试、写操作防重复和筛选空结果；首页支持 `/` 键快速聚焦搜索。
- 首页操作统一改为内联 SVG 图标，重构宽屏和 390px 窄窗口排版，保留深色森林材质和三类语义色。
- 软件内新增完全离线的“使用说明”页，以三条心法、三类工作流、七步连续例子和 P → A → M 诊断帮助零基础用户开始。
- 新增可独立阅读的 `docs/福格行为实验室-用户说明书.md`，包含详细示例、诊断表、常见误区、术语速查和专业支持边界。
- 新增 ADR-0033 和 ADR-0034，不改变数据库、备份、导入导出或离线运行边界。
- 在 `codex/anime-journal-ui` 分支增加可拆卸的《交响诗篇艾蕾卡7》主题：使用官网原版宣传视觉、记录来源与哈希，并以晴空、云白、冲浪青绿和机体珊瑚红建立清透的青春动画排版。
- 人物不再作为右侧独立海报，而是通过斜切页头、章节转场、低对比底纹和工作台横幅融入软件；界面装饰符号统一替换为本地 SVG 线性图标。
- 首页改为紧凑导航、一个当前行动主位和两个次要行动，三类工作流标题使用官方群像不同位置的裁切；移除 `PROJECTS / 行为设计`、`MINDSET` 等模板化英文小标签，并统一中日文无衬线字面与标题尺度。
- 动漫素材只作环境背景，运行时完全离线；本轮仅提供浏览器预览，不生成 EXE。

---

## 2026-08-22 · 聚焦式工作台与按需支持面板

- 长期习惯和一次性行为工作台取消常驻三列，中央当前步骤成为唯一主内容。
- 左侧摘要默认收起；右侧福格模型笔记改为覆盖抽屉，不再挤压中央表单。
- 一次性行为的三段路径移到中央顶部，形成稳定的流程定位。
- 理论笔记改为单项展开的手风琴，并补齐遮罩、Esc、焦点回收和键盘焦点约束。
- 以留白和步骤强调线替代一部分浅色卡片与边框；新增根目录 `DESIGN.md` 和 ADR-0021 固化设计语言。
- 保持本地字体、既有色彩与离线 SVG，不引入在线设计资源；本轮不生成安装包。
- 修复误用普通 `cargo build --release` 导致 EXE 请求 `localhost:1420` 的发布问题；明确便携版必须使用现有 `app/build-app.bat`，正式版继续完全离线运行。

## 2026-08-22 · 常驻设计状态与湖绿工作面

- 左侧由按需“设计摘要”改为默认常驻、可收起的“设计状态”，可在中央操作时持续对照七步结果；一次性行为同步使用常驻行为状态。
- 右侧福格模型笔记取消章节、单元来源小字，只保留知识点标题与精炼内容。
- 全局改为浅湖绿画布、雾白工作面和深青蓝操作色；纯白仅用于输入与少数抬升卡片，关键文字和交互色对比度均超过 4.5:1。
- 第二、四、五、六步引导压缩为单行；第三步地图扩大并压缩两轮说明；第七步实践历史与配方版本默认折叠。
- 状态栏增加纵向生长轨道、紧凑阶段标记和结果分组；窄窗口时降级为覆盖状态栏。
- 行为云朵的卡片移动、尺寸调整和焦点地图移动增加方向键替代操作。
- 新增 ADR-0022，并同步更新 `DESIGN.md`。

---

## 2026-08-21 · 第一阶段：知识库与设计

### 知识库构建
- 用 `scripts/extract_fogg_book.py` 从 360 页 PDF 提取文本，生成：
  - `knowledge/福格行为模型-全文.md`（全文合订，含页码锚点）
  - `knowledge/chapters/`（11 个分章）
  - `knowledge/page-index.jsonl`（每页一条 JSON，供 RAG/检索）
- 整理 `术语速查.md`、`图表与流程说明.md`。

### 参考库整理
- 写 `scripts/extract_references.py`，从附录清洗出：
  - `庆祝方式库`（100 条）
  - `微习惯配方库`（300 条，15 个场景）
- 人工转录 `肯定成功方式库`（表 9-1，8 框架 × 4 视角 = 32 条，原表为图片）。

### 设计文档
- `docs/blueprints/` 产出 5 份：软件大纲蓝图、七步工作流 PRD、概念数据模型与数据库草案、页面地图与线框图、MVP 迭代计划与验收标准。
- `docs/decisions/` 产出 ADR-0001 ~ 0005（本地优先、七步主工作流、决策文档化、技术栈、AI 策略）。

---

## 2026-08-21 · 第二阶段：MVP 实现（M0–M5）

### 环境搭建（M0）
- 安装 Rust 1.98（MSVC）与 Node 24 便携版；确认本机已有 VS Enterprise 2022 + Windows SDK。
- 封装 `app/build.bat`、`app/dev.bat`、`app/release.bat`（自动初始化 MSVC 环境）。
- 初始化 Tauri 2 + React + TS 项目；接入 SQLite（rusqlite bundled），14 张表迁移。

### 项目管理（M1）
- 后端：`habit_project` CRUD（新建/重命名/复制/归档/状态/步骤）。
- 前端：首页列表（筛选/搜索/分组）+ 字段级自动保存。

### 七步工作台（M2）
- 后端：`steps.rs`（愿望、行为集群、焦点地图、黄金行为、能力链、微行为、锚点、庆祝、配方生成）。
- 前端：六步组件 + 统一工作台框架（顶部进度 + 中央工作区 + 右侧辅助栏 + 底部导航）。

### 实践与诊断（M3）
- 后端：`practice.rs`（今日记录、提示→能力→动机纯规则诊断、配方版本历史）。
- 前端：第 7 步实践屏（六种结果快速记录 + 诊断建议 + 版本历史）。

### 参考库与辅助栏（M4）
- 参考库 JSON 打包进前端，接入第 2/5/6 步（配方启发、锚点模板、庆祝方式库）。
- 辅助栏随步骤变化，含书中要点与 PDF 页码。

### 数据管理（M5）
- 后端：`backups.rs`（VACUUM INTO 备份、恢复、JSON 导出）。
- 前端：数据管理页（备份/恢复/导出）。

### 打包
- `cargo tauri build` 生成 Windows 安装包（NSIS）。MSI 因 WiX 处理不了中文产品名失败，改用 NSIS 成功。

---

## 2026-08-21 · 第三阶段：收尾功能

- **数据导入**（ADR-0006）：`import_json` 按 uuid 项目级去重、外键 id 重映射、事务内执行。
- **自动滚动备份**（ADR-0007）：每次备份后只保留最近 7 份。
- **应用锁**（ADR-0008）：`settings` 表（迁移 002）+ sha2 加盐哈希密码 + 启动锁屏。
- 新增 ADR-0006 ~ 0008。
- 重新打包安装包，包含上述新功能。

---

## 2026-08-22 · 文档整理

- 编写根目录 `README.md`（项目总览）与本 `CHANGELOG.md`。
- 更新 `app/README.md`；清理空的 `tmp/` 目录。

---

## 2026-08-22 · 双工作流与完整交互重构

- 长期习惯工作台升级为三栏渐进式摘要，并支持多黄金行为与多微习惯分支。
- 第二至第四步加入行为集群云朵、可拖动 9×9 焦点地图、交互能力链和条件展开简化表单。
- 第一步改为一个核心愿望字段；第七步新增感受和情境记录。
- 新增完全本地的个人参考库，支持行为、配方、锚点、庆祝和肯定成功方式 CRUD。
- 首页与平行短字段采用响应式双列；窄窗口回退单列。
- 新增一次性行为快速入口、P→A→M 渐进诊断、每轮单因素调整、行动返回、历史与长期习惯转换。
- 数据库升级至 schema v4、28 张业务表；全量导出升级为 `fogg-lab-export-v3` 并兼容旧导出缺失的新表。
- 新增 ADR-0010 ~ ADR-0014 和双工作流实现记录。

### 画布与视觉体验补充

- 长期行为设计与一次性行为增加永久删除；删除前自动创建完整 SQLite 保护快照，并要求名称确认。
- 第二步行为卡片新增右下角缩放控制柄，宽高写入本地布局数据；新卡片根据文字长度采用更紧凑的初始尺寸。
- 第三步焦点地图坐标改为 −4～4，0 轴加粗，九级判断精度保持不变，并兼容旧评分。
- 七步标题、主要提示和首页分区加入离线符号图标、分步主题色和更明确的文本层级。
- 数据库升级至 schema v5；业务表数量仍为 28。
- 新增 ADR-0015。

### 黄金行为层级与焦点地图补充

- 黄金行为不再自动生成同名微习惯分支；进入第四步时才按需创建可独立命名的“微习惯方案 N”。
- 左侧摘要在第四步前展示延迟设计提示，进入第四步后支持添加与重命名微习惯方案，并增加层级图标。
- 焦点地图将整个第一象限标为黄金行为候选区，为正轴增加箭头，并将方向文字与刻度移到坐标轴附近。
- 高频迭代默认仅构建可直接运行的 EXE，本次未重新打包 NSIS 安装包。
- 新增 ADR-0016。

### 删除保护与条目删除补充

- 修复删除前保护快照使用非法 `backup_type='before-delete'` 导致的 SQLite CHECK 约束错误；保护快照现在以合法的完整备份登记。
- 新增微习惯方案、锚点候选、庆祝候选和本地备份删除命令。
- 已选黄金行为摘要增加移除选择；步骤内的个人行为、锚点、庆祝和肯定语言库增加就地删除。
- 保留配方版本、实践日志和诊断历史的回溯完整性，不提供普通单条删除。
- 无安装包构建已直接覆盖标准 `target/release/app.exe`；新增 ADR-0017。

### 紧凑布局与福格模型笔记

- 首页长期习惯的新建入口移到分区标题行，移除品牌副标题和多处重复浅灰说明。
- 第二步参考库紧邻行为输入，书中配方按身份折叠；提取行为浮层和云朵箭头重新压缩、强化。
- 第四至第六步用图标、并排结果和融合参考卡片减少纵向长度；庆祝评分改为行内选择。
- 第七步可选感受与情境默认折叠，流程末尾提供“完成本轮，回到首页”。
- 七步和一次性行为工作台接入按页面交叉匹配的“福格模型笔记”侧栏，原始提炼笔记未修改。
- 标准 EXE 已覆盖，新增 ADR-0018；本次未生成安装包。

### 按需展开与完整章节笔记

- 明确第七步返回首页只是结束本次记录，不代表习惯完成；按钮文案同步调整。
- 首页状态筛选改为下拉框，两个工作流分区默认收起，项目卡片改为宽屏 3–4 列自适应网格。
- 参考库使用覆盖式浮层，展开时不再推动下方卡片。
- 七步模型笔记展示完整相关章节；一次性行为根据 P/A/M 页面组合多个章节。
- 实践感受、情境、诊断和配方版本继续逐条持久化，并纳入备份和导出。
- 新增 ADR-0019；本次未生成安装包。

---

## 当前状态

- ✅ 知识库：360 页全文 + 11 分章 + 3 个结构化参考库
- ✅ 软件：长期习惯七步工作流 + 一次性行为 P→A→M 工作流 + 个人参考库 + 备份/导入/导出/应用锁
- ✅ 安装包：`app/src-tauri/target/release/bundle/nsis/福格行为实验室_0.1.0_x64-setup.exe`
- ⏳ 后续扩展：操作系统提醒策略、一次性任务行动会话与有限期项目（见 `docs/README.md`）
