# 概念数据模型与数据库草案

> 本文保留最初 14 实体的概念基线。当前实现已由 ADR-0010、ADR-0011、ADR-0014、ADR-0015 扩展到 schema v5、28 张业务表；准确表结构以 `app/src-tauri/migrations/001~005` 和[双工作流实现记录](../implementation/2026-08-22-长期习惯与一次性行为双工作流实现记录.md)为准。

- 文档版本：0.2
- 日期：2026-08-21
- 状态：草案（待评审）
- 上游依赖：`软件大纲蓝图.md` 第 9 节、`七步工作流详细产品需求文档.md`、ADR-0004（Tauri + React + SQLite）
- 原始范围：首版 MVP 的本地 SQLite 数据模型，覆盖 14 个实体；V0.2 实际数据库已扩展为 schema v7、33 张业务表，以迁移文件和实现记录为准

---

## 1. 目标与设计原则

1. **单用户本地库**：无账号、无网络（ADR-0001 / 0005），所有数据在设备上的 SQLite 文件中。
2. **可回溯**：配方每次变更生成新版本，修改保留原因（`project_change`）；实践记录绑定当时配方版本。
3. **无失败语义**：状态机不含"失败"；未执行进入诊断，不是道德评判。
4. **参考库只读**：庆祝方式、配方、肯定成功方式等内置内容不进 SQLite，由 `knowledge/references/*.json` 随应用只读加载；用户选用时把文本复制进用户实体并标注来源。
5. **字段级自动保存**：每个字段变更即写库，无需手动保存。

### 已确定的两项数据决策

- **主键策略（已决定，采用 C）**：本地主键用 `INTEGER PRIMARY KEY AUTOINCREMENT`，同时每个业务实体另设一列 `uuid TEXT NOT NULL UNIQUE` 作为跨设备/跨导出的稳定身份。本地表间外键仍用 INTEGER `id`（快、可读、可排序）；`uuid` 仅用于导出/导入去重与跨设备合并。`uuid` 在插入时由应用生成，建议用时间有序的 UUIDv7 或 ULID（可排序、索引局部性好）。
- **状态机字段拆分（已决定，采用 B）**：生命周期主线 `phase` 与"暂停/归档"两个正交开关分离；暂停/归档用时间戳字段表达（NULL=未发生），不设布尔，避免与底层 phase 冲突造成信息丢失。

## 2. 实体关系总览

```text
HabitProject 1 ──< Aspiration
HabitProject 1 ──< BehaviorOption 1 ── 1 FocusPlacement
HabitProject 1 ──< GoldenBehavior  >── 1 BehaviorOption
HabitProject 1 ──< AbilityAssessment
HabitProject 1 ──< TinyBehavior
HabitProject 1 ──< Anchor
HabitProject 1 ──< Celebration
HabitProject 1 ──< RecipeVersion  >── 0..1 ProjectChange
HabitProject 1 ──< PracticeEvent  >── 1 RecipeVersion
PracticeEvent 1 ──< ObstacleDiagnosis
HabitProject 1 ──< ObstacleDiagnosis
HabitProject 1 ──< ProjectChange
HabitProject 1 ──< BackupRecord
```

说明：除 `BackupRecord`（可为全量，不绑定项目）外，其余实体都归属于一个 `HabitProject`。`PracticeEvent` 强制引用 `RecipeVersion`，这是"结果可归因于哪一种设计"的关键约束。

## 3. 实体字段定义

> 每个业务实体统一含两列：`id`（INTEGER 主键，本地外键引用）与 `uuid`（TEXT，稳定身份）。时间戳统一为 TEXT（ISO-8601 UTC，如 `2026-08-21T12:00:00Z`）。

### 3.1 HabitProject（习惯项目）

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK | |
| uuid | TEXT | NOT NULL UNIQUE | 稳定身份 |
| name | TEXT | NOT NULL | 习惯名/工作标题 |
| aspiration_area | TEXT | 可空 | 愿望领域，用于首页分组 |
| phase | TEXT | draft/designing/ready/experimenting/stable | 生命周期 |
| paused_at | TEXT | 可空 | 非空表示已暂停 |
| archived_at | TEXT | 可空 | 非空表示已归档 |
| current_step | INTEGER | 1–7，可空 | 设计模式下当前步骤 |
| created_at / updated_at | TEXT | | |

### 3.2 Aspiration（愿望，第 1 步）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| uuid | TEXT NOT NULL UNIQUE | |
| project_id | FK → habit_project | |
| raw_input | TEXT | 初始"我想……" |
| input_type | TEXT | 愿望/成果/行为/不确定 |
| final_aspiration | TEXT | 最终愿望/成果 |
| why_important | TEXT | 为什么重要 |
| life_difference | TEXT | 改变后生活有何不同 |
| notes | TEXT | 补充说明 |
| created_at | TEXT | |

修改历史不另建表，由 `project_change` 记录（entity='aspiration'）。

### 3.3 BehaviorOption（行为选项，第 2 步）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| uuid | TEXT NOT NULL UNIQUE | |
| project_id | FK → habit_project | |
| text | TEXT NOT NULL | 可观察动作 |
| source | TEXT | 用户/模板/启发问题/内置库 |
| status | TEXT | 活跃/暂存/已删除（软删除） |
| notes | TEXT | |
| batch | TEXT | 生成批次标识 |
| sort_order | INTEGER | 排序 |
| created_at | TEXT | |

### 3.4 FocusPlacement（焦点地图定位，第 3 步）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| uuid | TEXT NOT NULL UNIQUE | |
| behavior_option_id | FK → behavior_option（1:1） | |
| impact | INTEGER 1–5 | 对愿望的影响 |
| feasibility | INTEGER 1–5 | 现在能否做到 |
| willing | INTEGER 0/1 | 是否愿意做 |
| pos_x / pos_y | REAL | 地图坐标（拖拽位置） |
| updated_at | TEXT | |

### 3.5 GoldenBehavior（黄金行为，第 3 步）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| uuid | TEXT NOT NULL UNIQUE | |
| project_id | FK → habit_project | |
| behavior_option_id | FK → behavior_option | 选定的黄金行为 |
| reason | TEXT | 选择理由 |
| is_active | INTEGER 0/1 | 是否当前推进（一次只推进 1 个） |
| created_at | TEXT | |

### 3.6 AbilityAssessment（能力评估，第 4 步）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| uuid | TEXT NOT NULL UNIQUE | |
| project_id | FK → habit_project | |
| time_factor / money_factor / energy_factor / brain_factor / schedule_factor | TEXT | 各自 充裕/一般/紧张 |
| weakest_link | TEXT | 时间/资金/体力/脑力/日程 |
| simplification_methods | TEXT（JSON 数组） | 提升技能/工具资源/缩小规模/入门步骤 |
| created_at | TEXT | |

### 3.7 TinyBehavior（微行为，第 4 步）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| uuid | TEXT NOT NULL UNIQUE | |
| project_id | FK → habit_project | |
| original_behavior | TEXT | 原行为描述 |
| tiny_behavior | TEXT | 微行为（缩小规模） |
| entry_step | TEXT | 入门步骤 |
| baseline | TEXT | 基线行为（必填） |
| optional_extension | TEXT | 可选扩展 |
| created_at | TEXT | |

### 3.8 Anchor（锚点，第 5 步）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| uuid | TEXT NOT NULL UNIQUE | |
| project_id | FK → habit_project | |
| anchor_text | TEXT NOT NULL | 候选锚点/既有事件 |
| last_action | TEXT | 锚点最后动作 |
| location | TEXT | 地点 |
| frequency | TEXT | 每日/每周/每月/不定 |
| source | TEXT | 用户/模板/内置库 |
| is_selected | INTEGER 0/1 | 是否选定锚点 |
| created_at | TEXT | |

### 3.9 Celebration（庆祝，第 6 步）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| uuid | TEXT NOT NULL UNIQUE | |
| project_id | FK → habit_project | |
| celebration_text | TEXT NOT NULL | 庆祝方式文本 |
| naturalness | INTEGER 1–5 | 自然度 |
| success_feeling | INTEGER 1–5 | 成功感 |
| source | TEXT | 用户/内置库 |
| is_selected | INTEGER 0/1 | 是否选定 |
| created_at | TEXT | |

### 3.10 RecipeVersion（配方版本，第 6/7 步）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| uuid | TEXT NOT NULL UNIQUE | |
| project_id | FK → habit_project | |
| version_number | INTEGER NOT NULL | 项目内自增（v1, v2, …） |
| anchor_last_action | TEXT | 锚点最后动作快照 |
| behavior_text | TEXT | 微行为/基线快照 |
| celebration_text | TEXT | 庆祝快照 |
| full_recipe_text | TEXT | 完整配方卡 |
| rehearsal_count | INTEGER | 演练次数 |
| rehearsal_feeling | TEXT | 演练感受 |
| change_id | FK → project_change（可空） | 触发本版本的变更；v1 为 NULL |
| status | TEXT | active/superseded |
| created_at | TEXT | |

### 3.11 PracticeEvent（实践事件，第 7 步）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| uuid | TEXT NOT NULL UNIQUE | |
| project_id | FK → habit_project | |
| recipe_version_id | FK → recipe_version（NOT NULL） | 记录时生效的配方版本 |
| result | TEXT | 自然完成/完成且多做/想起但没做/完全忘记/锚点没出现/不方便记录 |
| feeling | TEXT | 可选感受 |
| context | TEXT | 可选情境 |
| occurred_at | TEXT | 事件对应日期/时间 |
| created_at | TEXT | |

### 3.12 ObstacleDiagnosis（障碍诊断，第 7 步）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| uuid | TEXT NOT NULL UNIQUE | |
| project_id | FK → habit_project | |
| practice_event_id | FK → practice_event（可空） | 触发的实践事件 |
| obstacle_type | TEXT | 障碍分类 |
| diagnosis_path | TEXT | 提示/能力/动机 |
| suggestion | TEXT | 修改建议 |
| return_step | INTEGER 1–7 | 建议返回的步骤 |
| user_decision | TEXT | 用户最终决定 |
| created_at | TEXT | |

### 3.13 ProjectChange（变更记录）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| uuid | TEXT NOT NULL UNIQUE | |
| project_id | FK → habit_project | |
| entity | TEXT | 变更的实体名（aspiration/anchor/celebration/…） |
| field_name | TEXT | 变更字段 |
| old_value / new_value | TEXT | 变更前后值 |
| reason | TEXT | 变更原因 |
| created_at | TEXT | |

### 3.14 BackupRecord（备份记录）

> 本地簿记元数据，不参与用户数据导出，故不设 `uuid`。

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| project_id | FK（可空） | NULL 表示全量备份 |
| backup_type | TEXT | full/project |
| file_path | TEXT NOT NULL | |
| content_summary | TEXT | 恢复前显示的摘要 |
| schema_version | INTEGER | |
| created_at | TEXT | |

## 4. 枚举值清单

| 枚举 | 取值 |
|---|---|
| HabitProject.phase | draft, designing, ready, experimenting, stable |
| Aspiration.input_type | 愿望, 成果, 行为, 不确定 |
| BehaviorOption.source | 用户, 模板, 启发问题, 内置库 |
| BehaviorOption.status | 活跃, 暂存, 已删除 |
| 能力因素（五环节） | 充裕, 一般, 紧张 |
| AbilityAssessment.weakest_link | 时间, 资金, 体力, 脑力, 日程 |
| 简化方式 | 提升技能, 工具资源, 缩小规模, 入门步骤 |
| Anchor.frequency | 每日, 每周, 每月, 不定 |
| Anchor.source / Celebration.source | 用户, 模板/内置库 |
| RecipeVersion.status | active, superseded |
| PracticeEvent.result | 自然完成, 完成且多做, 想起但没做, 完全忘记, 锚点没出现, 不方便记录 |
| ObstacleDiagnosis.diagnosis_path | 提示, 能力, 动机 |
| BackupRecord.backup_type | full, project |

首页筛选与字段映射（正交模型）：

- 设计中 = `archived_at IS NULL AND paused_at IS NULL AND phase IN ('draft','designing')`
- 实践中 = `archived_at IS NULL AND paused_at IS NULL AND phase IN ('ready','experimenting')`
- 稳定 = `archived_at IS NULL AND paused_at IS NULL AND phase = 'stable'`
- 暂停 = `paused_at IS NOT NULL`
- 归档 = `archived_at IS NOT NULL`

## 5. SQLite 建表 DDL（草案）

```sql
PRAGMA foreign_keys = ON;

CREATE TABLE habit_project (
  id               INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid             TEXT NOT NULL UNIQUE,
  name             TEXT NOT NULL,
  aspiration_area  TEXT,
  phase            TEXT NOT NULL DEFAULT 'draft'
                   CHECK (phase IN ('draft','designing','ready','experimenting','stable')),
  paused_at        TEXT,
  archived_at      TEXT,
  current_step     INTEGER CHECK (current_step BETWEEN 1 AND 7),
  created_at       TEXT NOT NULL,
  updated_at       TEXT NOT NULL
);

CREATE TABLE aspiration (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT NOT NULL UNIQUE,
  project_id        INTEGER NOT NULL REFERENCES habit_project(id) ON DELETE CASCADE,
  raw_input         TEXT,
  input_type        TEXT CHECK (input_type IN ('愿望','成果','行为','不确定')),
  final_aspiration  TEXT,
  why_important     TEXT,
  life_difference   TEXT,
  notes             TEXT,
  created_at        TEXT NOT NULL
);

CREATE TABLE behavior_option (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid        TEXT NOT NULL UNIQUE,
  project_id  INTEGER NOT NULL REFERENCES habit_project(id) ON DELETE CASCADE,
  text        TEXT NOT NULL,
  source      TEXT NOT NULL DEFAULT '用户'
              CHECK (source IN ('用户','模板','启发问题','内置库')),
  status      TEXT NOT NULL DEFAULT '活跃'
              CHECK (status IN ('活跃','暂存','已删除')),
  notes       TEXT,
  batch       TEXT,
  sort_order  INTEGER NOT NULL DEFAULT 0,
  created_at  TEXT NOT NULL
);

CREATE TABLE focus_placement (
  id                 INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid               TEXT NOT NULL UNIQUE,
  behavior_option_id INTEGER NOT NULL UNIQUE REFERENCES behavior_option(id) ON DELETE CASCADE,
  impact             INTEGER CHECK (impact BETWEEN 1 AND 5),
  feasibility        INTEGER CHECK (feasibility BETWEEN 1 AND 5),
  willing            INTEGER CHECK (willing IN (0,1)),
  pos_x              REAL,
  pos_y              REAL,
  updated_at         TEXT NOT NULL
);

CREATE TABLE golden_behavior (
  id                 INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid               TEXT NOT NULL UNIQUE,
  project_id         INTEGER NOT NULL REFERENCES habit_project(id) ON DELETE CASCADE,
  behavior_option_id INTEGER NOT NULL REFERENCES behavior_option(id) ON DELETE CASCADE,
  reason             TEXT,
  is_active          INTEGER NOT NULL DEFAULT 1 CHECK (is_active IN (0,1)),
  created_at         TEXT NOT NULL
);

CREATE TABLE ability_assessment (
  id                    INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid                  TEXT NOT NULL UNIQUE,
  project_id            INTEGER NOT NULL REFERENCES habit_project(id) ON DELETE CASCADE,
  time_factor           TEXT CHECK (time_factor IN ('充裕','一般','紧张')),
  money_factor          TEXT CHECK (money_factor IN ('充裕','一般','紧张')),
  energy_factor         TEXT CHECK (energy_factor IN ('充裕','一般','紧张')),
  brain_factor          TEXT CHECK (brain_factor IN ('充裕','一般','紧张')),
  schedule_factor       TEXT CHECK (schedule_factor IN ('充裕','一般','紧张')),
  weakest_link          TEXT CHECK (weakest_link IN ('时间','资金','体力','脑力','日程')),
  simplification_methods TEXT,
  created_at            TEXT NOT NULL
);

CREATE TABLE tiny_behavior (
  id                 INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid               TEXT NOT NULL UNIQUE,
  project_id         INTEGER NOT NULL REFERENCES habit_project(id) ON DELETE CASCADE,
  original_behavior  TEXT,
  tiny_behavior      TEXT,
  entry_step         TEXT,
  baseline           TEXT,
  optional_extension TEXT,
  created_at         TEXT NOT NULL
);

CREATE TABLE anchor (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid         TEXT NOT NULL UNIQUE,
  project_id   INTEGER NOT NULL REFERENCES habit_project(id) ON DELETE CASCADE,
  anchor_text  TEXT NOT NULL,
  last_action  TEXT,
  location     TEXT,
  frequency    TEXT CHECK (frequency IN ('每日','每周','每月','不定')),
  source       TEXT NOT NULL DEFAULT '用户' CHECK (source IN ('用户','模板','内置库')),
  is_selected  INTEGER NOT NULL DEFAULT 0 CHECK (is_selected IN (0,1)),
  created_at   TEXT NOT NULL
);

CREATE TABLE celebration (
  id               INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid             TEXT NOT NULL UNIQUE,
  project_id       INTEGER NOT NULL REFERENCES habit_project(id) ON DELETE CASCADE,
  celebration_text TEXT NOT NULL,
  naturalness      INTEGER CHECK (naturalness BETWEEN 1 AND 5),
  success_feeling  INTEGER CHECK (success_feeling BETWEEN 1 AND 5),
  source           TEXT NOT NULL DEFAULT '用户' CHECK (source IN ('用户','内置库')),
  is_selected      INTEGER NOT NULL DEFAULT 0 CHECK (is_selected IN (0,1)),
  created_at       TEXT NOT NULL
);

CREATE TABLE recipe_version (
  id                 INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid               TEXT NOT NULL UNIQUE,
  project_id         INTEGER NOT NULL REFERENCES habit_project(id) ON DELETE CASCADE,
  version_number     INTEGER NOT NULL,
  anchor_last_action TEXT,
  behavior_text      TEXT,
  celebration_text   TEXT,
  full_recipe_text   TEXT,
  rehearsal_count    INTEGER,
  rehearsal_feeling  TEXT,
  change_id          INTEGER REFERENCES project_change(id) ON DELETE SET NULL,
  status             TEXT NOT NULL DEFAULT 'active' CHECK (status IN ('active','superseded')),
  created_at         TEXT NOT NULL,
  UNIQUE (project_id, version_number)
);

CREATE TABLE practice_event (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT NOT NULL UNIQUE,
  project_id        INTEGER NOT NULL REFERENCES habit_project(id) ON DELETE CASCADE,
  recipe_version_id INTEGER NOT NULL REFERENCES recipe_version(id),
  result            TEXT NOT NULL
                    CHECK (result IN ('自然完成','完成且多做','想起但没做','完全忘记','锚点没出现','不方便记录')),
  feeling           TEXT,
  context           TEXT,
  occurred_at       TEXT,
  created_at        TEXT NOT NULL
);

CREATE TABLE obstacle_diagnosis (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT NOT NULL UNIQUE,
  project_id        INTEGER NOT NULL REFERENCES habit_project(id) ON DELETE CASCADE,
  practice_event_id INTEGER REFERENCES practice_event(id) ON DELETE SET NULL,
  obstacle_type     TEXT,
  diagnosis_path    TEXT CHECK (diagnosis_path IN ('提示','能力','动机')),
  suggestion        TEXT,
  return_step       INTEGER CHECK (return_step BETWEEN 1 AND 7),
  user_decision     TEXT,
  created_at        TEXT NOT NULL
);

CREATE TABLE project_change (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid        TEXT NOT NULL UNIQUE,
  project_id  INTEGER NOT NULL REFERENCES habit_project(id) ON DELETE CASCADE,
  entity      TEXT,
  field_name  TEXT,
  old_value   TEXT,
  new_value   TEXT,
  reason      TEXT,
  created_at  TEXT NOT NULL
);

CREATE TABLE backup_record (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id      INTEGER REFERENCES habit_project(id) ON DELETE SET NULL,
  backup_type     TEXT NOT NULL CHECK (backup_type IN ('full','project')),
  file_path       TEXT NOT NULL,
  content_summary TEXT,
  schema_version  INTEGER,
  created_at      TEXT NOT NULL
);
```

## 6. 索引

```sql
CREATE INDEX idx_aspiration_project   ON aspiration(project_id);
CREATE INDEX idx_behavior_project     ON behavior_option(project_id, status);
CREATE INDEX idx_golden_project       ON golden_behavior(project_id, is_active);
CREATE INDEX idx_anchor_project       ON anchor(project_id, is_selected);
CREATE INDEX idx_celebration_project  ON celebration(project_id, is_selected);
CREATE INDEX idx_recipe_project       ON recipe_version(project_id, version_number);
CREATE INDEX idx_practice_project     ON practice_event(project_id, occurred_at);
CREATE INDEX idx_practice_recipe      ON practice_event(recipe_version_id);
CREATE INDEX idx_diag_project         ON obstacle_diagnosis(project_id);
CREATE INDEX idx_change_project       ON project_change(project_id, entity);
```

> `uuid` 列均带 `UNIQUE` 约束（SQLite 自动建唯一索引），无需额外索引。

## 7. 迁移与 schema 版本

- 用 SQLite `PRAGMA user_version` 记录 schema 版本；迁移为一系列有序 SQL，在事务内执行，任一步失败即回滚。
- 迁移只做结构变更，不丢用户数据；新增字段优先给默认值，删除字段前先做数据迁移。
- 首版从 `user_version = 0` 开始，首次建库执行 `v1` 迁移（即上表 DDL）。
- 具体迁移机制、备份保留策略、加密/应用锁仍需单独 ADR（见 `docs/README.md` 仍待决策）。

## 8. 参考库（只读）与用户选用

- 内置参考数据（`knowledge/references/*.json`：32 种肯定成功方式、100 种庆祝方式、300 个微习惯配方）随应用打包、只读加载，不进 SQLite。
- 用户选用时，把条目文本**复制**进用户实体，并标注来源：
  - 庆祝 → `celebration.celebration_text` + `source='内置库'`；
  - 配方示例 → 启发后成为 `behavior_option.text` + `source='内置库'`；
  - 锚点模板 → `anchor.anchor_text` + `source='模板'`；
  - 肯定成功方式 → 用于第 6 步"感受成功"引导文案，不落库（或落库为庆祝文案）。
- 通过"文本复制 + 来源标注"而非外键指向 JSON，保证用户数据自包含、可独立导出。

## 9. 备份与导出（指向后续 ADR）

- 全量备份 = SQLite 文件快照 + 可选 JSON 导出；单习惯导出 = 该项目全部 13 张业务表的行。
- 导出采用开放格式（JSON/Markdown），实践记录内嵌当时的 `recipe_version` 快照，保证可读、可验证。
- 导出/导入用 `uuid` 做身份匹配与去重，导入时重映射 INTEGER 主键。
- 具体导出格式、导入、跨设备迁移需单独 ADR。

## 10. 待决策项

- 数据库迁移与 schema 版本管理细节；
- 自动备份保留策略与是否加密/应用锁；
- 单习惯与全量数据的导出/导入格式。
